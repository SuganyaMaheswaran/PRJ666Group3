apiVersion: v1
kind: Service
metadata:
  name: settlecan-backend
  labels:
    app: settlecan-backend
spec:
  type: ClusterIP
  selector:
    app: settlecan-backend
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
