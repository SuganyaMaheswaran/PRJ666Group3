apiVersion: v1
kind: Service
metadata:
  name: settlecan-frontend
  labels:
    app: settlecan-frontend
spec:
  type: ClusterIP
  selector:
    app: settlecan-frontend
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP